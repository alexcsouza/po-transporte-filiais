    
### Bibliotecas
#### Python
    $ pip install numpy
    $ pip install pandas
    $ pip install pyomo
    $ pip install 'pyomo[optional]'
    $ pip install glpsol
    $ pip install highspy
    $ pip install gurobipy

#### Outras bibliotecas
    $ sudo apt install glpk-utils